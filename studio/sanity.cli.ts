import { defineCliConfig } from 'sanity/cli'

export default defineCliConfig({
  projectId: 'YOUR_PROJECT_ID', // TODO: Replace with real ID — run `npx sanity@latest init` or create at https://sanity.io
  dataset: 'production',
})
